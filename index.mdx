---
title: "API Registro Pendientes: ISP Field Work Order Management"
description: "REST API for managing ISP field operations — work orders, technician assignments, client records, payments, and real-time notifications."
---

API Registro Pendientes is a Spring Boot REST API built for internet service providers to manage field work orders (pendientes), track technician activity, record payments, and push real-time notifications to mobile and web clients. All endpoints are secured with JWT bearer tokens and documented via Swagger UI.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Make your first authenticated API call in minutes.
  </Card>
  <Card title="Authentication" icon="lock" href="/authentication">
    Learn how to obtain and use JWT bearer tokens.
  </Card>
  <Card title="Core Concepts" icon="lightbulb" href="/concepts/pendientes">
    Understand work orders, states, and permissions.
  </Card>
  <Card title="API Reference" icon="code" href="/api/auth">
    Full endpoint documentation with request and response schemas.
  </Card>
</CardGroup>

## What you can do

<CardGroup cols={2}>
  <Card title="Manage work orders" icon="clipboard-list" href="/api/pendientes">
    Create, assign, track, and close field work orders across five types: fault repair, internet installation, camera installation, device pickup, and relocation.
  </Card>
  <Card title="Track technician time" icon="clock" href="/api/pendientes-tiempos">
    Open and close time-tracking sessions per technician per work order, with start/end timestamps and session descriptions.
  </Card>
  <Card title="Real-time notifications" icon="bell" href="/guides/websockets">
    Subscribe to WebSocket topics over STOMP for instant updates when work orders are assigned, postponed, or completed.
  </Card>
  <Card title="Upload field notes" icon="image" href="/guides/file-uploads">
    Technicians can attach notes and photos to work orders; images are uploaded via multipart form and served statically.
  </Card>
</CardGroup>

## How it works

<Steps>
  <Step title="Authenticate">
    Call `POST /auth` with your DNI and password to receive a JWT bearer token.
  </Step>
  <Step title="Create a work order">
    Call `POST /api/pendientes` with client ID, type, priority, and location details.
  </Step>
  <Step title="Assign a technician">
    Call `PUT /api/pendientes/{id}/asignar` with the employee ID to move the order to `ASIGNADO` state.
  </Step>
  <Step title="Track progress">
    The technician advances state through `EN_PROGRESO`, `POR_VALIDAR`, and finally `FINALIZADO` using `PUT /api/pendientes/{id}/estado`.
  </Step>
</Steps>

<Note>
  All endpoints except `/auth/**` and `/app/**` require a valid JWT bearer token in the `Authorization` header. See [Authentication](/authentication) for details.
</Note>
